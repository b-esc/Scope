{
  "name": "scope",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel ft-client/ -d lib/",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "flow-bin": "^0.131.0"
  },
  "scripts": {
    "flow": "flow"
  }
}
